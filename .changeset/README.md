# Changesets

Drop a markdown file here for every release-worthy change. Run `pnpm changeset` and follow the prompt — it will write the file for you.

See the [changesets docs](https://github.com/changesets/changesets) for details.
